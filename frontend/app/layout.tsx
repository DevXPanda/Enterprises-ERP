import type { Metadata } from "next";
import "./globals.css";
import { AppShell } from "@/components/layout/app-shell";

export const metadata: Metadata = {
  title: "NKTech ERP — Enterprise Manufacturing Platform",
  description:
    "Premium enterprise manufacturing ERP dashboard for NKTech Enterprises. Manage production, wages, quality control, and more.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <AppShell>{children}</AppShell>
      </body>
    </html>
  );
}
